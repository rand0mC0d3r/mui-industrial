import { styled } from '@mui/material/styles';
export var SNotifications = styled('div')(function () { return ({
    gap: '0px',
    display: 'flex',
    right: '16px',
    zIndex: 112,
    overflow: 'scroll',
}); });
