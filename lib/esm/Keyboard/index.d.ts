/// <reference types="react" />
import { ShortcutObject } from '../index.types';
export default function ({ id, shiftKey, ctrlKey, commandAltKey, ascii, char, label, insensitive, onTrigger, }: ShortcutObject): JSX.Element;
