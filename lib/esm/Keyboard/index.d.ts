/// <reference types="react" />
import { ShortcutObject } from '../index.types';
export default function ({ id, shiftKey, ctrlKey, altKey, metaKey, ascii, char, label, insensitive, onTrigger, }: ShortcutObject): JSX.Element;
