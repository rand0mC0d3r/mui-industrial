import { ShortcutObject } from '../index.types';
export declare const RegisterShortcut: ({ id, ascii, char, label, ctrlKey, altKey, metaKey, shiftKey, insensitive, onTrigger, }: ShortcutObject) => void;
